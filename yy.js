const arr= [24,2,17,83,64];
function isOdd(x){
    return x % 2 ==1;
}
 function includer(arr, isOdd){
     const narr=[];
     for (let i=0; i<arr.length;i++){
         if(isOdd(arr))
     }
 }
