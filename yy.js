const arr= [5,8,14,17,23,6,8];
function isOdd(x){
    return x % 2 ==1;
}
 function includer(arr, isOdd){
     const narr=[];
     for (let i=0; i<arr.length;i++){
         if(isOdd(arr))
     }
 }